language: python
python:
 - "3.5"
install:
 - pip install coveralls
addons:
  apt:
    packages:
    - nasm
    - binutils
    - gcc
script: coverage run -m unittest discover
after_success: coveralls
